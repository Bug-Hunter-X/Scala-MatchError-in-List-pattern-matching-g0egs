# Scala MatchError in List Pattern Matching

This repository demonstrates a peculiar MatchError encountered while using pattern matching with a Scala List.  The error is inconsistent, as similar pattern matching elsewhere in the code works correctly.

The bug is highlighted in `bug.scala`.  The solution, explaining the root cause and offering a fix, is in `bugSolution.scala`.